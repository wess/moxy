void main() {
  // range iteration
  int sum = 0;
  for i in 0..5 {
    sum += i;
  }
  assert(sum == 10);

  // range with different start
  int sum2 = 0;
  for i in 3..6 {
    sum2 += i;
  }
  assert(sum2 == 12);
}
