Future<int> double_it(int x) {
  return x * 2;
}

Future<int> add_one(int x) {
  return x + 1;
}

void main() {
  int a = await double_it(10);
  int b = await add_one(a);
  assert(a == 20)
  assert(b == 21)
}
