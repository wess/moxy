#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

int add(int a, int b);
int square(int x);


int add(int a, int b) {
    return a + b;
}

int square(int x) {
    return x * x;
}

int main(void) {
    printf("%d\n", add(3, 4));
    printf("%d\n", square(5));
    return 0;
}

