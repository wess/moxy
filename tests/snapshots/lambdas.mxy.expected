#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

static inline int __moxy_lambda_0(int x) {
    return x * 2;
}

static inline int __moxy_lambda_1(int x) {
    int y = x + 10;
    return y * 2;
}

int apply(int x, int(*fn)(int));


int apply(int x, int(*fn)(int)) {
    return fn(x);
}

int main(void) {
    int doubled = apply(5, __moxy_lambda_0);
    printf("%d\n", doubled);
    int result = apply(3, __moxy_lambda_1);
    printf("%d\n", result);
    return 0;
}

