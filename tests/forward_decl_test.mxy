// forward declarations
int compute(int x);
int double_it(int x);

int compute(int x) {
  return double_it(x) + 1;
}

int double_it(int x) {
  return x * 2;
}

void main() {
  int a = compute(10);
  assert(a == 21)

  int b = double_it(5);
  assert(b == 10)
}
