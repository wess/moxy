void main() {
    int[] nums = [1, 2, 3];
    nums.push(4);
    assert(nums.len == 4);
    assert(nums[0] == 1);
    assert(nums[3] == 4);

    map[string,int] m = {};
    m.set("a", 1);
    m.set("b", 2);
    assert(m.get("a") == 1);
    assert(m.len == 2);
}
